#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int a, b, c, d, result;

    cin >> a >> b >> c >> d;

    result = (a*b) - (c*d);

    cout << "Difference = " << result << endl;

    return 0;
}